import { JoinTypesPipe } from './join-types.pipe';

describe('JoinTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
